import{notFound}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{PageHeader}from"@/components/ui/page-header";import{Card}from"@/components/ui/card";import{ProformaFromOfferForm}from"@/components/forms/proforma-from-offer-form";
export default async function OfferProforma({params}:{params:Promise<{id:string}>}){const{id}=await params;const s=await createClient();const{data}=await s.from("offers").select("offer_number,client_id,clients(company_name)").eq("id",id).single();if(!data)notFound();return <><PageHeader title="Tekliften proforma" description={`${data.offer_number} · ${(data.clients as unknown as{company_name:string})?.company_name||"Önce lead’i müşteriye dönüştürün"}`}/><Card className="max-w-2xl p-6"><ProformaFromOfferForm offerId={id}/></Card></>}
