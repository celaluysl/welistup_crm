import{createClient}from"@/lib/supabase/server";import{PageHeader}from"@/components/ui/page-header";import{Card}from"@/components/ui/card";import{OfferForm}from"@/components/forms/offer-form";
export default async function NewOffer(){const s=await createClient();const[{data:clients},{data:leads},{data:services},{data:profiles}]=await Promise.all([s.from("clients").select("id,company_name").eq("status","active").order("company_name"),s.from("leads").select("id,company_name").not("status","in",'(won,lost)').order("company_name"),s.from("services").select("id,name").eq("status","active").order("name"),s.from("profiles").select("id,first_name,last_name,email").eq("status","active").order("first_name")]);return <><PageHeader title="Yeni teklif" description="Müşteri veya lead için teklif ve ilk revizyonu oluşturun."/><Card className="p-6"><OfferForm clients={clients||[]} leads={leads||[]} services={services||[]} profiles={profiles||[]}/></Card></>}
