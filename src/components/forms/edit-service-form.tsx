"use client";
import{useActionState,useEffect}from"react";import{updateService}from"@/lib/actions/core";import{Button}from"@/components/ui/button";import{Field,inputClass}from"@/components/ui/field";
type Service={id:string;name:string;category:string|null;description:string|null;default_periodicity:"monthly"|"one_time"|"periodic";status:"active"|"inactive"};
export function EditServiceForm({service,onSuccess}:{service:Service;onSuccess?:()=>void}){const[state,action,pending]=useActionState(updateService,null);useEffect(()=>{if(state?.success)onSuccess?.()},[state?.success,onSuccess]);return <form action={action} className="grid gap-4 sm:grid-cols-2"><input type="hidden" name="id" value={service.id}/><Field label="Hizmet adı"><input name="name" defaultValue={service.name} required className={inputClass}/></Field><Field label="Kategori"><input name="category" defaultValue={service.category||""} className={inputClass}/></Field><Field label="Varsayılan periyot"><select name="default_periodicity" defaultValue={service.default_periodicity} className={inputClass}><option value="monthly">Aylık</option><option value="one_time">Tek seferlik</option><option value="periodic">Dönemsel</option></select></Field><Field label="Durum"><select name="status" defaultValue={service.status} className={inputClass}><option value="active">Aktif</option><option value="inactive">Pasif</option></select></Field><Field label="Açıklama" className="sm:col-span-2"><textarea name="description" defaultValue={service.description||""} rows={4} className={`${inputClass} h-auto py-2`}/></Field>{(state?.error||state?.success)&&<p className={`text-sm sm:col-span-2 ${state.error?"text-red-600":"text-emerald-600"}`}>{state.error||state.success}</p>}<div className="sm:col-span-2 flex justify-end"><Button disabled={pending}>{pending?"Kaydediliyor…":"Değişiklikleri kaydet"}</Button></div></form>}
