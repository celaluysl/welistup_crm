"use client";
import{useActionState}from"react";import{updateProfileAccess}from"@/lib/actions/core";import{Button}from"@/components/ui/button";import{inputClass}from"@/components/ui/field";
export function ProfileAccessForm({profileId,roleId,employmentType,status,roles}:{profileId:string;roleId:string|null;employmentType:string;status:string;roles:{id:string;name:string}[]}){const[state,action,pending]=useActionState(updateProfileAccess,null);return <form action={action} className="grid gap-2 lg:grid-cols-[170px_170px_130px_auto]"><input type="hidden" name="profile_id" value={profileId}/><select aria-label="Rol" name="role_id" defaultValue={roleId||""} required className={inputClass}><option value="">Rol seçin</option>{roles.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select><select aria-label="Çalışma tipi" name="employment_type" defaultValue={employmentType} className={inputClass}><option value="partner">Ortak</option><option value="employee">Bordrolu çalışan</option><option value="freelancer">Freelancer</option><option value="outsourced">Dış kaynak</option><option value="other">Diğer</option></select><select aria-label="Durum" name="status" defaultValue={status} className={inputClass}><option value="active">Aktif</option><option value="inactive">Pasif</option><option value="archived">Arşiv</option></select><Button variant="secondary" disabled={pending}>Güncelle</Button>{(state?.error||state?.success)&&<p className={`text-xs lg:col-span-4 ${state.error?"text-red-600":"text-emerald-600"}`}>{state.error||state.success}</p>}</form>}
