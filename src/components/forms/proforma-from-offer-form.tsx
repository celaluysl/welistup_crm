"use client";
import{useActionState}from"react";import{createProformaFromOffer}from"@/lib/actions/offers";import{Button}from"@/components/ui/button";import{Field,inputClass}from"@/components/ui/field";
export function ProformaFromOfferForm({offerId}:{offerId:string}){const[state,action,pending]=useActionState(createProformaFromOffer,null);return <form action={action} className="grid gap-4 sm:grid-cols-2"><input type="hidden" name="offer_id" value={offerId}/><Field label="Proforma tarihi"><input name="issue_date" type="date" required defaultValue={new Date().toISOString().slice(0,10)} className={inputClass}/></Field><Field label="Geçerlilik tarihi"><input name="valid_until" type="date" className={inputClass}/></Field><Field label="Banka bilgileri" className="sm:col-span-2"><textarea name="bank_details" rows={4} className={`${inputClass} h-auto py-2`} placeholder="Banka, IBAN ve hesap sahibi"/></Field>{state?.error&&<p className="sm:col-span-2 text-sm text-red-600">{state.error}</p>}<div className="sm:col-span-2 flex justify-end"><Button disabled={pending}>{pending?"Oluşturuluyor…":"Proforma oluştur"}</Button></div></form>}
