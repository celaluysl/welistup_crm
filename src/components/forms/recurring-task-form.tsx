"use client";
import{useActionState}from"react";import{createRecurringTemplate}from"@/lib/actions/operations";import{Button}from"@/components/ui/button";import{Field,inputClass}from"@/components/ui/field";
export function RecurringTaskForm({projectId,services}:{projectId:string;services:{id:string;name:string}[]}){const[state,action,pending]=useActionState(createRecurringTemplate,null);return <form action={action} className="grid gap-4 sm:grid-cols-2"><input type="hidden" name="project_id" value={projectId}/><Field label="Görev başlığı" className="sm:col-span-2"><input name="title" required className={inputClass}/></Field><Field label="Hizmet"><select name="project_service_id" className={inputClass}><option value="">Genel proje görevi</option>{services.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></Field><Field label="Öncelik"><select name="priority" className={inputClass}><option value="normal">Normal</option><option value="low">Düşük</option><option value="high">Yüksek</option><option value="urgent">Acil</option></select></Field><Field label="Tekrar"><select name="recurrence" className={inputClass}><option value="daily">Günlük</option><option value="weekly">Haftalık</option><option value="monthly">Aylık</option><option value="custom">Özel periyot</option></select></Field><Field label="Her kaç periyotta"><input name="interval_value" type="number" min="1" defaultValue="1" className={inputClass}/></Field><Field label="Başlangıç"><input name="starts_on" type="date" required className={inputClass}/></Field><Field label="Bitiş (opsiyonel)"><input name="ends_on" type="date" className={inputClass}/></Field><Field label="Açıklama" className="sm:col-span-2"><textarea name="description" rows={3} className={`${inputClass} h-auto py-2`}/></Field>{(state?.error||state?.success)&&<p className={`sm:col-span-2 text-sm ${state.error?"text-red-600":"text-emerald-600"}`}>{state.error||state.success}</p>}<div className="sm:col-span-2 flex justify-end"><Button disabled={pending}>Şablon oluştur</Button></div></form>}
