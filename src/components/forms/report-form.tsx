"use client";
import{useActionState}from"react";import{createReport}from"@/lib/actions/operations";import{Button}from"@/components/ui/button";import{Field,inputClass}from"@/components/ui/field";
export function ReportForm({projectId,services}:{projectId:string;services:{id:string;name:string}[]}){const[state,action,pending]=useActionState(createReport,null);return <form action={action} className="grid gap-4 sm:grid-cols-2"><input type="hidden" name="project_id" value={projectId}/><Field label="Rapor başlığı" className="sm:col-span-2"><input name="title" required className={inputClass}/></Field><Field label="Dönem"><input name="period" type="date" required className={inputClass}/></Field><Field label="Hizmet"><select name="project_service_id" className={inputClass}><option value="">Genel proje raporu</option>{services.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></Field><Field label="Durum"><select name="status" className={inputClass}><option value="planned">Hazırlanacak</option><option value="preparing">Hazırlanıyor</option><option value="ready">Hazır</option><option value="sent">Gönderildi</option><option value="approved">Onaylandı</option></select></Field><Field label="Rapor URL"><input name="url" type="url" placeholder="https://" className={inputClass}/></Field><Field label="Not" className="sm:col-span-2"><textarea name="notes" rows={3} className={`${inputClass} h-auto py-2`}/></Field>{(state?.error||state?.success)&&<p className={`sm:col-span-2 text-sm ${state.error?"text-red-600":"text-emerald-600"}`}>{state.error||state.success}</p>}<div className="sm:col-span-2 flex justify-end"><Button disabled={pending}>Rapor oluştur</Button></div></form>}
